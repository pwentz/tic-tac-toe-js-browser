#### Tic Tac Toe

```
npm install
```

##### Browser UI

After `npm install`ing, run:

```
npm run build
```

Open `index.html` in your browser.


##### Terminal UI

After `npm install`ing, run:

```
node terminalUI/main.js
```
